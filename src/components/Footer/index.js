import React from "react";
import { Flex, Text } from "rebass";

const Footer = props => {
    return (
        <Flex>
            <Text>Footer</Text>
        </Flex>
    );
};

export default Footer;
